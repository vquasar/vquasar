/target
**/*.rs.bk
*.pdb

# Runtime state (should never be committed)
/run
*.sock
*.pid

# Local dev config overrides
config/local.toml
.env

# Editor / OS noise
.DS_Store
*.swp
.idea/
.vscode/

# Local AI/agent tooling (never published)
/CLAUDE.md
/.claude/

# UI (added in Milestone 5)
/ui/node_modules
/ui/dist
design_*
# Brand-guideline archives are design source, not repo content — the exported
# assets that the console actually uses live in ui/public/.
*.zip
